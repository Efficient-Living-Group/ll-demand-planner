{
  "name": "ll-demand-planner",
  "version": "1.0.0",
  "description": "Little Lifely Demand Planning Dashboard",
  "main": "server.js",
  "scripts": {
    "start": "npm run validate && node server.js",
    "validate": "node --check server.js && node scripts/validate_deploy_gate.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "ws": "^8.20.0"
  }
}
